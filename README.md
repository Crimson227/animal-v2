# 流浪动物爱心管理系统

## 项目简介
本项目是《数据库系统》课程期末大作业。系统旨在为流浪动物收容所提供一套高效的信息管理解决方案，通过数字化手段实现对动物档案、医疗记录、人员管理及领养寄养业务的全流程追踪，替代低效的纸质记录。

## 核心功能
* **动物档案管理**：录入、查询、修改和删除动物基本信息（物种、品种、年龄、照片等），支持按状态（待领养/已领养）筛选。
* **医疗健康监控**：
    * **疫苗管理**：追踪疫苗库存及接种记录。
    * **处方管理**：记录患病动物的用药方案。
* **人员与领养**：管理爱心人士（领养人/寄养人）信息，办理领养与寄养手续，自动更新动物状态。
* **数据完整性**：后端集成数据库触发器，在动物被领养时自动维护数据一致性。

## 技术栈
* **前端**：HTML5, CSS3 (Flexbox/Grid), JavaScript (ES6+), Handlebars 模板引擎
* **后端**：Node.js, Express 框架
* **数据库**：MySQL (支持触发器与存储过程)

## 快速开始

### 1. 环境准备
确保本地已安装以下环境：
* Node.js (建议 v14+)
* MySQL 数据库 (建议 v8.0+)

### 2. 数据库配置
1.  登录 MySQL，创建一个新的数据库（例如 `animal_shelter`）。
2.  运行项目根目录下的 `animal_shelter_DDL.sql` 脚本。该脚本会自动创建表结构、视图、触发器、存储过程，并导入初始化中文测试数据。
3.  打开 `mysql-db-connector.js` 文件，填入你的数据库连接信息：

        var pool = mysql.createPool({
            connectionLimit : 10,
            host            : 'localhost',
            user            : 'root',      // 你的MySQL用户名
            password        : '123456',    // 你的MySQL密码
            database        : 'animal_shelter' // 你的数据库名
        })

### 3. 安装依赖
在项目根目录下打开终端，运行：

    npm install

### 4. 启动项目
使用以下命令启动 MySQL 版本服务器：

    npm run start-mysql


### 5. 访问系统
服务器启动成功后，打开浏览器访问：
`http://localhost:27469`

## 作者信息
* **学院**：软件与物联网工程学院
* **专业**：软件工程（中外）
* **学号**：0234163、0234165、0234164
* **姓名**：胡圣哲、舒兆宸、毛俊杰